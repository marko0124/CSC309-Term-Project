import React from 'react';
// import apiClient from '../api/client';

const LogIn = () => {
    
    return (
        <div>
        <h1>Log In</h1>
        <button>Log In</button>
        </div>
    );
}

export default LogIn;